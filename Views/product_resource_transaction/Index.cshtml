@model IEnumerable<furniture.Models.product_resource_transaction>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.initial_amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.product.id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.product_resource.id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.initial_amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.product.id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.product_resource.id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
